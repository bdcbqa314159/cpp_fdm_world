project(excel_sdk_api)

set(includes includes/)
set(sources src/framework.c
            src/MemoryManager.cpp
            src/MemoryPool.cpp
            src/xlcall.cpp
)

add_library(${PROJECT_NAME} ${sources})
target_include_directories(${PROJECT_NAME} PUBLIC ${includes} ${common_includes_dir})